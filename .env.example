VITE_SUPABASE_PROJECT_ID="your_project_ref_here"
VITE_SUPABASE_URL="https://your-project-ref.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="your_anon_public_key_here"

# Optional: connection string (DO NOT put real secrets in example)
SUPABASE_DB_URL="postgresql://postgres:password@db.your-project-ref.supabase.co:5432/postgres"

# Notes:
# - Copy this to `.env` and replace values with real secrets for local development.
# - Ensure `.env` is listed in `.gitignore` (already included in this repo).